const ps = require('../modules/ps');

const solution = (n) => {
  let answer = '';

  return answer;
};

ps.main(async () => {
  let n = parseInt(await ps.readLine()); // 문자열을 입력 받아 정수 반환

  console.log(solution(n));
});
